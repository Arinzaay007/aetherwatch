# ═══════════════════════════════════════════════════════════════
# AetherWatch — Environment Variables
# Copy this file to .env and fill in your credentials.
# NEVER commit .env to version control.
# ═══════════════════════════════════════════════════════════════

# ── OpenSky Network (FREE) ──────────────────────────────────
# Register at: https://opensky-network.org/index.php?option=com_users&view=registration
# Anonymous access works but is rate-limited (10 req/10s vs 100 req/10s with account)
OPENSKY_USERNAME=
OPENSKY_PASSWORD=

# ── NASA Earthdata (FREE) ───────────────────────────────────
# Register at: https://urs.earthdata.nasa.gov/users/new
# Required for some GIBS layers; basic WMS works without auth
NASA_EARTHDATA_USERNAME=
NASA_EARTHDATA_PASSWORD=

# ── FlightRadar24 (OPTIONAL UPGRADE — PAID) ─────────────────
# Subscribe at: https://fr24api.flightradar24.com
# Leave blank to use OpenSky (free) instead
FR24_API_TOKEN=

# ── Alert: Email (OPTIONAL) ─────────────────────────────────
# For email anomaly alerts (uses Gmail SMTP or any SMTP server)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
ALERT_EMAIL_TO=

# ── Alert: Twilio SMS (OPTIONAL) ────────────────────────────
# Sign up at: https://www.twilio.com/try-twilio (free trial)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=
TWILIO_TO_NUMBER=

# ── App Settings ────────────────────────────────────────────
# Set to "true" to always use mock data (useful for demos without internet)
FORCE_MOCK_DATA=false
# Logging level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO
